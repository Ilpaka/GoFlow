# Локальные значения для docker compose и приложения (не коммитьте реальные секреты).
POSTGRES_USER=messenger
POSTGRES_PASSWORD=messenger
POSTGRES_DB=messenger

HTTP_PORT=8080
POSTGRES_DSN=postgres://messenger:messenger@localhost:5432/messenger?sslmode=disable
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0
JWT_SECRET=change-me-in-development-only
JWT_TOKEN_TTL_SECONDS=3600
